name: Flow Phase 3 certification
on:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  flow-phase3:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    container:
      image: mcr.microsoft.com/playwright/python:v1.57.0-noble
      options: --ipc=host
    env:
      HOME: /root
    steps:
      - uses: actions/checkout@v4
      - name: Install browser test dependencies
        run: python3 -m pip install -r tests/browser/requirements.txt
      - name: Certify Flow Phase 3 browser gameplay
        run: python3 tests/browser/flow_phase3.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: flow-phase3-evidence
          path: browser-artifacts/flow-phase3/
          retention-days: 7
