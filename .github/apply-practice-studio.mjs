import fs from 'node:fs';
import assert from 'node:assert/strict';
import {execFileSync} from 'node:child_process';
function replace(file,before,after){const source=fs.readFileSync(file,'utf8');assert.equal(source.split(before).length,2,`Expected one finishing marker in ${file}`);fs.writeFileSync(file,source.replace(before,after));}
const identity='js/practiceLab/practiceLabIdentity.js';
replace(identity,"${drawings[id] ?? drawings['full-assessment']}","${Object.hasOwn(drawings, id) ? drawings[id] : drawings['full-assessment']}");
replace(identity,'  if (drawings[id] && title) {','  if (Object.hasOwn(drawings, id) && title) {');
replace(identity,"    const wrapper = document.createElement('div'); wrapper.className = 'practice-lab-shell';\n    main.before(wrapper); main.classList.remove('practice-lab-shell'); wrapper.append(main); shell = wrapper;","    const active = document.activeElement;\n    const wrapper = document.createElement('div'); wrapper.className = 'practice-lab-shell';\n    main.before(wrapper); main.classList.remove('practice-lab-shell'); wrapper.append(main); shell = wrapper;\n    if (main.contains(active)) active.focus?.({preventScroll:true});");
replace(identity,"'Why this plan?']);","'Why this plan?','Interpretation boundary','Recommended keys','Recommended targets','Recommended words']);");
const css=`\n/* Final layout pass: route headings stay announced, controls retain visible focus. */\n.practice-lab-screen :is(h1,h2)[tabindex='-1']:focus { outline:none; }\n.practice-lab-screen .practice-lab-detail { margin:0 auto; }\n.practice-lab-screen :is(.practice-custom-library,.practice-custom-editor) { border:1px solid var(--pl-rule);border-radius:4px;background:var(--pl-surface);padding:20px; }\n.practice-lab-screen .practice-lab-detail dl:is(.practice-real-text-result-grid,.practice-weak-key-result-grid,.practice-combination-result-grid) { grid-template-columns:repeat(4,minmax(0,1fr));gap:0;padding:0;border:0;border-top:1px solid var(--pl-rule);border-radius:0;background:none; }\n.practice-lab-screen :is(.practice-real-text-result-grid,.practice-weak-key-result-grid,.practice-combination-result-grid)>div { border:0;border-bottom:1px solid var(--pl-rule);border-radius:0;padding:22px 18px;min-width:0;background:transparent; }\n.practice-lab-screen :is(.practice-real-text-result-grid,.practice-weak-key-result-grid,.practice-combination-result-grid) dd { margin:10px 0 0;font:600 26px/1.2 var(--font-game,monospace);overflow-wrap:anywhere; }\n.practice-lab-screen[data-practice-view='custom-text-result'] .practice-real-text-result-grid>div:nth-child(-n+2) dd { font-size:42px;letter-spacing:-.065em; }\n.practice-lab-screen[data-practice-view='custom-text-result'] .practice-real-text-result-grid>div:first-child dd { color:var(--pl-accent); }\n@media(max-width:700px) {\n  .practice-lab-screen .practice-lab-detail dl:is(.practice-real-text-result-grid,.practice-weak-key-result-grid,.practice-combination-result-grid) { grid-template-columns:repeat(2,minmax(0,1fr)); }\n  .practice-lab-screen :is(.practice-real-text-result-grid,.practice-weak-key-result-grid,.practice-combination-result-grid)>div { padding:17px 12px; }\n  .practice-lab-screen :is(.practice-real-text-result-grid,.practice-weak-key-result-grid,.practice-combination-result-grid) dt { font-size:11px;line-height:1.5; }\n  .practice-lab-screen :is(.practice-real-text-result-grid,.practice-weak-key-result-grid,.practice-combination-result-grid) dd { font-size:23px; }\n  .practice-lab-screen[data-practice-view='custom-text-result'] .practice-real-text-result-grid>div:nth-child(-n+2) dd { font-size:36px; }\n}\n`;
fs.appendFileSync('practiceLabIdentity.css',css);
const test='tests/browser/practice_identity.mjs';
replace(test,"if(['weak-keys','custom-text','full-assessment','numbers-symbols'].includes(id))await shot(`${id}-setup`);","if(['weak-keys','custom-text','full-assessment','numbers-symbols'].includes(id)){await shot(`${id}-setup`);await page.screenshot({path:path.join(out,`${browserName}-${width}-${id}-full.png`),fullPage:true});}");
replace('tests/practice-lab-identity.test.js',"assert.ok(!practiceDrillArtwork('weak-keys').includes('<image'));","assert.ok(!practiceDrillArtwork('weak-keys').includes('<image'));assert.equal(practiceDrillArtwork('toString'),practiceDrillArtwork('full-assessment'));");
execFileSync('git',['add','practiceLabIdentity.css',test,'tests/practice-lab-identity.test.js']);
fs.rmSync('.github/apply-practice-studio.mjs');
